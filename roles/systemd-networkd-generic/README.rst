systemd-network-generic
=======================

Configure, start, and enable ``systemd-networkd``.

This role configures ``systemd-networkd`` so that it will automatically use any
wired interface with DHCP.
