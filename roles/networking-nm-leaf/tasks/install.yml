---
- block:

  - name: Install NetworkManager
    pacman:
      name: networkmanager
      state: present

  - name: Start and enable NetworkManager
    systemd:
      name: NetworkManager.service
      state: started
      enabled: true

  become: true
