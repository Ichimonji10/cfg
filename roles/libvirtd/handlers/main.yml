---
- name: Set install reason for packages
  command: 'pacman -D --asdeps dmidecode openbsd-netcat qemu'
  become: true

- name: Restart libvirtd
  systemd:
    name: libvirtd.service
    state: restarted
  become: true
