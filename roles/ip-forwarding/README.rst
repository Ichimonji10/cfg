ip-forwarding
=============

Enable or disable IP forwarding.

Currently, this role manages only IPv4 forwarding. By default, this role
disables forwarding. Set the ``ip_forwarding`` variable to true to enable
forwarding.
