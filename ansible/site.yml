---
# workstations & servers ###########################################################################

- hosts: workstations
  roles:
    - borg_backup_users
    - dev_env
    - workstation

- hosts: servers
  roles:
    - sshd

# other ############################################################################################

- hosts: all
  roles:
    - crda
    - essential_apps
    - lm_sensors
    - locale
    - net_nm
    - nft
    - pacman_conf
    - smartd
    - time
    - ucode
    - user_ichimonji10

- hosts: apc_hosts
  roles:
    - apcupsd

- hosts: btrfs_hosts
  roles:
    - btrfs_balance
    - btrfs_scrub
    - btrfs_subvolume

- hosts: coreboot_hosts
  roles:
    - coreboot

- hosts: syncthing_hosts
  roles:
    - syncthing

# Requisite package is unavailable
# - hosts: uefi_hosts
#   roles:
#     - uefi

- hosts: vm_hosts
  roles:
    - dockerd
    - libvirtd
