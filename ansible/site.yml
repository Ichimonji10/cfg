---
# workstations & servers ###########################################################################

- hosts: workstations
  roles:
    - borg_backup_users
    - net_nm
    - workstation

- hosts: servers
  roles:
    - net_sd  # could use net_nm, for sake of homogeneity?
    - sshd

# other ############################################################################################

- hosts: all
  roles:
    - dockerd
    - essential_apps
    - libvirtd
    - lm_sensors
    - nft
    - smartd
    - syncthing
    - user_ichimonji10

- hosts: apc_hosts
  roles:
    - apcupsd
