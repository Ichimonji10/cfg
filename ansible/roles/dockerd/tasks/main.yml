---
- block:

    - name: Install docker
      pacman:
        name: docker

    - name: Install docker configuration file
      copy:
        src: daemon.json
        dest: /etc/docker/
        mode: 0600
      notify: Restart docker

    - name: Start and enable dockerd
      systemd:
        name: docker.service
        enabled: true

  become: true
